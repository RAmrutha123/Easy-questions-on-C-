#include<stdio.h>
int main()
{
    int a,b,s1=0,s2=0,i;
    scanf("%d",&a);
    scanf("%d",&b);
    for(i=1;i<=a/2;i++)
    if(a%i==0)
    {
        s1+=i;
    }
    for(i=1;i<=b/2;i++)
    if(b%i==0)
    {
        s2+=i;
    }
    printf((s1==b && s2==a)?"Amicable":"Not Amicable");
    return 0;
}
