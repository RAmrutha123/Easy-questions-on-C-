
#include<stdio.h>
int main()
{
    int n,rem,sum=0,p=1;
    scanf("%d",&n);
    int num=n;
    while(n!=0)
    {
      rem=n%10;
      sum+=rem;
      p*=rem;
      n=n/10;
    }
    if(sum==p)
      printf("Spy Number");
    else
      printf("Not Spy Number");
    return 0;
}
