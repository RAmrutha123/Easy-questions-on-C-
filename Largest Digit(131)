
#include<stdio.h>
int main()
{
    int n,rem,a=0;
    scanf("%d",&n);
    while(n!=0)
    {
        rem=n%10;
        if(rem>a)
        {
          a=rem;
        }
        n=n/10;
    }
    printf("%d",a);
    return 0;
}
