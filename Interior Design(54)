
#include<stdio.h>
int main()
{
    int x1,y1,x2,y2;
    scanf("%d %d %d %d",&x1,&y1,&x2,&y2);
    int z1=x1+y1;
    int z2=x2+y2;
    if(z1>z2)
     printf("%d",z2);
    else
     printf("%d",z1);
    return 0;

}
