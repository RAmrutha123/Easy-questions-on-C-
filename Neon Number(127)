
#include<stdio.h>
int main()
{
    int n,rem,sum=0;
    scanf("%d",&n);
    int a=n*n;
    while(a>0)
    {
        rem=a%10;
        sum+=rem;
        a=a/10;
    }
    if(n==sum)
      printf("Neon Number");
    else
      printf("Not Neon Number");
    return 0;
}
